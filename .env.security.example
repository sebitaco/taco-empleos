# Cookie Security Configuration
# Copy relevant variables to your .env.local file

# Force secure cookies even in development (useful for HTTPS testing)
# FORCE_SECURE_COOKIES=true

# Cookie domain restriction (leave undefined for current domain only)
# Useful for subdomain sharing but can reduce security
# COOKIE_DOMAIN=.yourdomain.com

# Enable strict SameSite for all cookies (highest security)
# May break some integrations that require cross-site requests
# STRICT_SAME_SITE=true

# JWT Secrets (REQUIRED - change these in production!)
JWT_SECRET=your-secret-key-change-in-production-use-long-random-string
CSRF_SECRET=your-csrf-secret-change-in-production-use-different-random-string

# Session Configuration
# SESSION_EXPIRY_DAYS=7

# Security Headers Configuration
# CSP_REPORT_URI=https://your-csp-report-endpoint.com/report
# HSTS_MAX_AGE=31536000

# Development Security Testing
# Set to true when testing with HTTPS in development
# DEV_HTTPS_TESTING=false

# Cookie Consent Configuration (if implementing cookie consent)
# COOKIE_CONSENT_REQUIRED=true
# ANALYTICS_COOKIES_ENABLED=false
# MARKETING_COOKIES_ENABLED=false

# Additional Security Options
# COOKIE_PREFIX_LEVEL=highest  # options: basic, medium, high, highest
# ENABLE_COOKIE_AUDIT=true     # Enable cookie security auditing in dev mode

# Production Security Hardening
# These should ONLY be enabled in production with proper testing
# ENABLE_CERTIFICATE_TRANSPARENCY=true
# ENABLE_PUBLIC_KEY_PINNING=false  # Requires careful certificate management